<!DOCTYPE html>
<html>
<head>
  <title>The Splurty App, by Jim Hinckley</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="col-xs-3">
      <%= link_to root_path, class: 'logo' do %>
        <div class="circle">
          Jim's<br />SPLURTY<br/>App!
        </div>
      <% end %>
    </div>
    <div class="col-xs-4 hidden-xs slogan">
        Splurting out clever quotes and dubious advice for nearly a hundredth of a century
    </div>
    <div class="col-xs-offset-1 col-sm-offset-0 col-xs-5 links">
      <!-- Button to trigger modal -->
      <%= link_to 'Contribute', '#', data: {toggle: 'modal', target: '#myModal'} %>
      <%= link_to 'About', about_path %>
    </div>
  </header>


  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Contribute Your Quote:</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
        <div class="modal-body">
            <%= f.input :saying, label: "Type the quote you want to display (3 to 200 characters):", placeholder: "Text of quote" %><br />
            <%= f.input :author, label: "Name of person who said it (3 to 200 characters):", placeholder: "Who said it?" %>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Add Quote', class: 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <hr />

  <br class="clear" />

  <div class="container">
  <% if flash[:error].present? %>
    <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
      <%= flash[:error].html_safe %>
    </div>
    <% end %>
    <%= yield %>
  </div>

  <div class="footer">
    <div class="container">
      <div class="col-xs-12 col-sm-6">
        <%= link_to "This isn't enough, I need more", root_path, class: 'next-quote' %>
      </div>
      <div class="col-xs-12 col-sm-6 footer-blurb">
      <p class="text-muted">This app is brought to you by <a href="http://www.jhwm.net">Jim Hinckley</a>, web developer and Firehose Project student.</p>
      </div>
    </div>
  </div>

</body>
</html>
